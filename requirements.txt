# ============================================================
# AuditLens — requirements.txt
# Unpinned versions — let pip resolve compatible set
# Install: pip install -r requirements.txt
# ============================================================

# --- Web & API ---
requests
httpx

# --- PDF & Document Processing ---
pdfplumber
PyMuPDF
beautifulsoup4
lxml

# --- NLP & Claim Detection ---
spacy
# After install run: python -m spacy download en_core_web_sm

# --- Transformers & ML ---
transformers>=4.35.0,<4.40.0     # pin below 4.40 — later versions eagerly import torchvision
torch                            # CPU-only; conda installed separately
datasets
evaluate
scikit-learn
numpy
pandas

# --- Explainability ---
shap

# --- Embeddings (CPU-only) ---
sentence-transformers
faiss-cpu

# --- RAG & LLM ---
# pin these together — they have a tight version coupling
langchain
langchain-core
langchain-google-genai
langchain-community
google-genai>=2.0.0               # new Google Gen AI SDK (replaces google-generativeai)

# --- MLOps ---
evidently
mlflow
dvc
dvc-gs

# --- Cloud & Storage ---
google-cloud-storage
google-cloud-run

# --- Dashboard ---
streamlit
plotly
reportlab

# --- HuggingFace Hub ---
huggingface-hub

# --- Testing ---
pytest
pytest-cov
pytest-mock

# --- Config & Utilities ---
pydantic>=2.0
pydantic-settings
python-dotenv
PyYAML
loguru
tqdm
